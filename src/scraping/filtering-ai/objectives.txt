first, introduce pages to the getPaths, so it gets the routes from a web
Then, the first filter would be, get the correct paths for then getting the correct classes.
From the given classes, the info to get would be the one that it's on the constPages
` 
armyTech: new SiteConfig({
    name: "ArmyTech",
    siteImage: "/images/stores/armytech.png",
    isPcComponent: true, <- This would be set to true if it provides components for a pc, latops, monitors, etc. Armytech would be an example
    isSetup: false, <- This would be set to true if it provides decorations, chairs, desktops, keycaps, leds, etc. MGM gamers would be an example
    baseUrl: "https://www.armytech.com.ar", <- baseUrl
    categories: [ <- this could be more or less cattegories/paths/routes. If there is the chance of getting all products from a route, like a "/*" route or "all-products" would be very easy, since it's not needing to see each path, at the same time this would be dangerous, resulting of not getting all products. categories: ["/search/?q=*"],
      "/405-hardware",
      "/436-perifericos",
      "/398-gaming-house",
      "/431-monitores-y-tvs",
      "/373-conectividad",
      "/389-energia", <- A good way to see if the paths were correctly selected, it's getting the classes to similar paths, and if they are slightly different, there are correct.
    ],
    selectors: { <- From the getClassesFromPath,
      productWrapper: "cgw-product-card", <- The container of the infop
      title: "h3.product-card__title", <- name/title of the product
      price: ".product-card__cart__price--current", <- price of the product
      link: "a", <- the anchor tag. 
      image: ".medium product-card__image img", <- Image of the product
    },
    pagination: {
      type: "queryParam", <- Type of pagination. Could be query or path. This right now it's giving problem, since there are cases that scraps the same page, "thinking that" it's changing the page.
      param: "cate",
    },
    isOutOfStock: (element) => element.textContent.includes("Agotado"),
    maxPages: 1, <- Max pages of each category (Possible indiviual max pages.)
    )},
`

With the json response, each page would have an individual separation of each
product category. Example, have video cards, outlet video cards. I can save a lot of 
time, money, etc. Instead of filtering things by myself.

there is the possibility of ["/search/?q=*"] that works and display all the products of the page.